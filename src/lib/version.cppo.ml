(* This is the version given in _oasis. These preprocessor macros are
   computed by cppo (see Makefile) *)
let version = VERSION
let has_git_tag = HAS_GIT_TAG
let git_tag = GIT_TAG
let has_yices2 = HAS_YICES2